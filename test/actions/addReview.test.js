import dotenv from 'dotenv'
import { expect } from 'chai'
import { addReview } from '../../src/actions'

dotenv.config()

describe('function addReview ', () => {
  it('should create a new row in the ratings table', () => {
    // TODO: FIXME and write a real test
    expect(1).to.equal(2)
  })
})
