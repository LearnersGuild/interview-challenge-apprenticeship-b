import '../env'
import { expect } from 'chai';
import { addRating } from '../../src/actions';

describe('function addRating ', () => {
  it('should create a new row in the ratings table', () => {
    // TODO: FIXME and write a real test
    expect(1).to.equal(2)
  })
})
